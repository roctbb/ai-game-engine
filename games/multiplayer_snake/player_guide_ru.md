# Командная змейка

В этой игре один игрок управляет отрядом из четырех змеек. Для каждой роли `snake_1`, `snake_2`, `snake_3`, `snake_4` нужно написать функцию `make_move(x, y, board)`.

- `x`, `y` — координаты головы вашей змейки.
- `board` — общая матрица поля.

Значения в `board`:

- `0` — пустая клетка.
- `1` — еда.
- `-1` — препятствие: край поля или тело любой змейки.

Функция возвращает `up`, `down`, `left` или `right`.

```python
def make_move(x, y, board):
    for action, dx, dy in [("right", 1, 0), ("down", 0, 1), ("left", -1, 0), ("up", 0, -1)]:
        nx = x + dx
        ny = y + dy
        if 0 <= ny < len(board) and 0 <= nx < len(board[ny]) and board[ny][nx] != -1:
            return action
    return "right"
```

Все змейки игрока ходят одновременно.

Правила столкновений:

- Ход в край поля или тело любой змейки выбивает участника.
- Если несколько змеек идут в одну клетку, все они выбывают.
- Разворот на 180 градусов блокируется: вместо него остается прежнее направление.
- В освобождающийся хвост своей змейки заходить можно, если змейка в этот ход не ест.

Итоговый счет игрока — сумма очков всех четырех змеек. Еда важнее всего, затем длина и выживание.
