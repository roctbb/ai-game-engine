# Выход из лабиринта

Напишите функцию `make_move(state)`, которая на каждом шаге возвращает одно действие: `up`, `down`, `left` или `right`.

В `state` приходят:

- `position`: текущая позиция агента, например `{"x": 1, "y": 1}`.
- `exit`: координаты выхода (случайное место на каждом запуске).
- `size`: размер карты.
- `walls`: список стен.
- `step`: номер текущего шага.

Цель: дойти до выхода за меньшее число шагов и не пытаться ходить в стены. Коридоры узкие (1 клетка), весь лабиринт окружён стенами, поэтому путь часто надо продумывать, а не «перепрыгивать» через пустые участки.
Карта теперь крупнее — это лабиринт 31×31, и финиш выбирается случайно по достижимой клетке с длинным маршрутом от старта.
За скорость движения начисляется больше очков: чем ближе ваш путь к оптимальному, тем выше результат.
За некорректные действия (неизвестное действие / шаг в стену) даются штрафы.

Для отладки можно использовать `print(...)`. Вывод появится в консоли под визуализацией синхронно с кадрами replay.
